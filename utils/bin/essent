#!/bin/bash

# This may be a brittle way to find $(root_dir)/utils/bin, is there a better way?
#JAVA_FLAGS="-Xmx100G -Xms10G  -agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=5005"
path=`dirname "$0"`
exec java ${JAVA_FLAGS} -cp ${path}/essent.jar essent.Driver ${@:1}
